export const RenderMessages = async(usersObject, messagesObject) => {

    for (const key in usersObject) {
        
        const element = usersObject[key];
        console.log(key)
        console.log(element)
        
        for (const element of messagesObject) {
            
        }

    }

}